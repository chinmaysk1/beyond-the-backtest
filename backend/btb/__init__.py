"""Beyond the Backtest -- a guided strategy discovery and validation engine.

The project computes locally and end to end: market data in, an
out-of-sample-validated strategy with a plain-English robustness verdict out.
Week 1 builds the bottom of that stack -- a reproducible, audited local store of
market data, because every statistic above it inherits whatever is wrong down
here.
"""

__version__ = "0.1.0"
