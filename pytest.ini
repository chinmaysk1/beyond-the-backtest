# Tests live in backend/ but should run from the repo root too, so CI and a
# plain `pytest` at the top level both work without a cd.
[pytest]
testpaths = backend/tests
pythonpath = backend
addopts = -q
